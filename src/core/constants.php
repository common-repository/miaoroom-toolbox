<?php
// 更多精品WP资源尽在喵容：miaoroom.com

defined( 'MR_DS' ) || define( 'MR_DS', DIRECTORY_SEPARATOR );

defined( 'MRTB_VERSION' ) || define( 'MR_THEME_VERSION', '0.0.1' );

defined( 'MRTB_DIR' ) || define( 'MRTB_DIR',  trailingslashit(dirname(dirname( dirname( __FILE__ ) ))) );

defined( 'MRTB_DIR_LOADER' ) || define( 'MRTB_DIR_LOADER', MRTB_DIR . 'src/loaders/' );

defined( 'MRTB_DIR_ASSETS_IMAGE' ) || define( 'MRTB_DIR_ASSETS_IMAGE', MRTB_DIR . 'src/assets/images/' );
